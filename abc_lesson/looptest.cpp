#include <bits/stdc++.h>
using namespace std;

int main () {
    int N = 5;
    vector <int> s(N);
    for (auto &a : s) {
        cin >> a;
    }
    for (auto a : s) {
        cout << a << endl;
    }
}
